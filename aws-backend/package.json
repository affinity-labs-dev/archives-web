{
  "name": "archives-stripe-webhook",
  "version": "1.0.0",
  "description": "AWS Lambda handler for Stripe webhooks - Archives Expo subscription management",
  "main": "src/lambda.js",
  "scripts": {
    "test": "node test/test-webhook.js",
    "deploy": "npm run package && aws lambda update-function-code --function-name archives-stripe-webhook --zip-file fileb://webhook-handler.zip",
    "package": "zip -r webhook-handler.zip src/ node_modules/ package.json"
  },
  "keywords": [
    "stripe",
    "webhook",
    "aws-lambda",
    "subscription",
    "archives-expo"
  ],
  "author": "Archives Expo",
  "license": "MIT",
  "dependencies": {
    "stripe": "^14.21.0",
    "@supabase/supabase-js": "^2.39.7"
  },
  "devDependencies": {
    "@aws-sdk/client-lambda": "^3.490.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}